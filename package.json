{
  "name": "buddies-tube",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "concurrently \"cd backend && yarn start\" \"cd frontend && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
